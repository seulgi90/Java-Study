package ch11;

public class study {

  // data = 값 +  타입 ( 타입이 없다면 값의 의미를 알수 없다)

  // collections Framework : 다수의 객체(data, 컬렉션)를 다루기 쉽게 표준화된 프로그래밍 방식

  // 순차 탐색 , 이진 탐색(정렬 필수)

  // List
  // - 순서 유지o,  중복o
  // vector , list, LinkedList
  // vector.clear() =  저장된 모든 요소 제거

  // Set
  // - 순서 유지x, 중복x

  // Map (key, value)
  // - 순서 유지x, 중복(키x, 값o)
}
